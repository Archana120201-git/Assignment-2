FROM node:16

# Install dependencies for Ethereum development
RUN npm install -g truffle ganache-cli

# Install Remix CLI if needed
RUN npm install -g @remix-project/remixd

# Set the working directory
WORKDIR /workspace

CMD ["bash"]
